{
  "name": "composable-search",
  "version": "1.0.0",
  "description": "Composable Search DAG System",
  "private": true,
  "workspaces": [
    "sdk",
    "ui",
    "playground",
    "app"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev:sdk": "npm run dev --workspace=sdk",
    "dev:ui": "npm run dev --workspace=ui",
    "dev:playground": "npm run dev --workspace=playground",
    "dev:app": "npm run dev --workspace=app",
    "build:sdk": "npm run build --workspace=sdk",
    "build:ui": "npm run build --workspace=ui",
    "build:app": "npm run build --workspace=app"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
